import {useEffect,useState} from "react";import {api} from "../../lib/api";import {ClipboardList,Download} from "lucide-react";
export default function AdminReports(){const [rows,setRows]=useState<any[]>([]);const [loading,setLoading]=useState(true);useEffect(()=>{api<any>("/admin/transactions").then(d=>setRows(d.transactions||[])).finally(()=>setLoading(false))},[]);function download(){const headers=["id","userId","service","customer","amount","commission","date","status"];const csv=[headers.join(","),...rows.map(r=>headers.map(h=>JSON.stringify(r[h]??"")).join(","))].join("\n");const a=document.createElement("a");a.href=URL.createObjectURL(new Blob([csv],{type:"text/csv"}));a.download=`ld-service-zone-report-${new Date().toISOString().slice(0,10)}.csv`;a.click();URL.revokeObjectURL(a.href)}return <div className="p-6 space-y-6 max-w-[1300px]"><div className="flex items-center justify-between gap-4"><div><h1 className="font-display text-2xl font-extrabold">Reports</h1><p className="text-[#94A3B8] text-sm">Export recorded transactions. No demo rows are included.</p></div><button disabled={loading||!rows.length} onClick={download} className="inline-flex items-center gap-2 px-4 py-2.5 rounded-xl bg-[#1D56D8] text-white text-sm font-semibold disabled:opacity-40"><Download size={16}/>Export CSV</button></div>{loading?<div className="h-40 bg-white rounded-2xl animate-pulse"/>:<div className="bg-white rounded-2xl border border-[#E2E8F0] p-10 text-center"><ClipboardList className="mx-auto text-[#94A3B8]"/><h2 className="font-semibold mt-3">{rows.length?`${rows.length} recorded transactions ready`:"No report data yet"}</h2><p className="text-sm text-[#94A3B8] mt-1">Reports are generated only from real backend records.</p></div>}</div>}
